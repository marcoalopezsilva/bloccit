<h1>New Topic</h1>

  <div class="row">
    <div class="col-md-4">
      <p>Guidelines for topics:</p>
        <ul>
           <li>Make sure the topic is appropriate.</li>
           <li>Never insult dogs.</li>
           <li>Smile when you type.</li>
         </ul>
    </div>
    <div class="col-md-8">
      <!-- Next block used 'f' but I substituted with 'field' to make sure I understand how it works-->
      <%= form_for @topic do |field| %>
        <div class="form-group">
          <%= field.label :name %>
          <%= field.text_field :name, class: 'form-control', placeholder: "Enter topic name" %>
        </div>
        <div class="form-group">
          <%= field.label :description %>
          <%= field.text_area :description, rows: 8, class: 'form-control', placeholder: "Enter topic description" %>
        </div>
        <div class="form-group">
          <%= field.label :public, class: 'checkbox' do %>
            <%= field.check_box :public %> Public topic
          <% end %>
       </div>
          <%= field.submit "Save", class: 'btn btn-success' %>
          <% end %>
    </div>
 </div>

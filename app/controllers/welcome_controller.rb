class WelcomeController < ApplicationController
  # WelcomeController originally contained two (empty) methods: 'index' and 'about'. There have the same names as corresponding views.
  # (So, inside app/views/welcome there must be a view called 'index')
  def index
  end

  def about
  end
end
